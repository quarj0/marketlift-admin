import Link from "next/link";
import { reports } from "@/data/mock-data";
import { StatusBadge } from "@/components/ui/status-badge";
import { AdminButton } from "@/components/ui/admin-button";
import { Icons } from "@/lib/icons";
export default async function ReportDetailPage({params}:{params:Promise<{reportId:string}>}){const {reportId}=await params;const report=reports.find(x=>x.id===reportId)??reports[0];return <div className="space-y-6"><Link href="/reports" className="text-xs font-bold text-slate-500">← Back to reports</Link><div className="flex flex-col gap-4 rounded-xl border border-slate-200 bg-white p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><h1 className="text-xl font-black">{report.id}</h1><StatusBadge status={report.status}/></div><p className="mt-1 text-xs text-slate-500">{report.reason} · {report.created}</p></div><div className="flex gap-2"><AdminButton>Resolve report</AdminButton><AdminButton variant="outline">Dismiss</AdminButton></div></div><div className="grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><div className="space-y-6"><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-sm font-black">Reported content</h2><div className="mt-4 rounded-xl bg-slate-50 p-5"><p className="text-[10px] font-bold uppercase tracking-wide text-slate-400">{report.type}</p><h3 className="mt-2 text-base font-black text-slate-900">{report.target}</h3><p className="mt-2 text-xs leading-5 text-slate-500">The reported marketplace content is shown here for administrator investigation. Open the original content and seller history before deciding.</p><button className="mt-4 inline-flex items-center gap-2 text-xs font-black text-emerald-700"><Icons.external size={14}/> Open content</button></div></section><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-sm font-black">Reporter statement</h2><blockquote className="mt-4 rounded-lg border-l-4 border-orange-400 bg-orange-50/60 p-4 text-xs leading-6 text-slate-700">“The seller asked me to make a payment outside the platform and the information in the listing does not appear to match the product they showed me.”</blockquote></section></div><aside className="space-y-6"><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-sm font-black">Report details</h2><div className="mt-4 space-y-4 text-xs"><Row label="Reporter" value={report.reporter}/><Row label="Reason" value={report.reason}/><Row label="Priority" value={report.priority}/><Row label="Target type" value={report.type}/><Row label="Created" value={report.created}/></div></section><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-sm font-black">Internal note</h2><textarea className="mt-3 min-h-28 w-full rounded-lg border border-slate-200 p-3 text-xs outline-none focus:border-emerald-500" placeholder="Add investigation notes..."/><button className="mt-2 text-xs font-black text-emerald-700">Save note</button></section></aside></div></div>}
function Row({label,value}:{label:string;value:string}){return <div className="flex justify-between gap-4"><span className="text-slate-500">{label}</span><strong className="text-right text-slate-800">{value}</strong></div>}
