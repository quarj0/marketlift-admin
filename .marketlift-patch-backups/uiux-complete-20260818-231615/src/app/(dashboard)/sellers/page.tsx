import Link from "next/link";
import { sellers } from "@/data/mock-data";
import { PageHeader } from "@/components/ui/page-header";
import { TableTools } from "@/components/ui/table-tools";
import { StatusBadge } from "@/components/ui/status-badge";
import { AdminButton } from "@/components/ui/admin-button";
import { Icons } from "@/lib/icons";
export default function SellersPage(){return <div className="space-y-6"><PageHeader title="Sellers" description="Manage seller accounts, plans and marketplace standing." actions={<AdminButton variant="outline"><Icons.download size={16}/> Export sellers</AdminButton>}/><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[["Total sellers","4,281"],["Verified","3,918"],["Pending verification","18"],["Suspended","42"]].map(([a,b])=><div key={a} className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-xs font-semibold text-slate-500">{a}</p><strong className="mt-2 block text-xl font-black text-slate-950">{b}</strong></div>)}</div><div className="overflow-hidden rounded-xl border border-slate-200 bg-white"><TableTools placeholder="Search seller, owner or seller ID..."><button className="inline-flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-3 text-xs font-bold text-slate-600"><Icons.filter size={15}/> Plan</button><button className="inline-flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-3 text-xs font-bold text-slate-600">Status <Icons.arrowDown size={12}/></button></TableTools><div className="table-scroll"><table className="admin-table"><thead><tr><th>Seller</th><th>Plan</th><th>Listings</th><th>Rating</th><th>Revenue</th><th>Status</th><th/></tr></thead><tbody>{sellers.map(s=><tr key={s.id}><td><div className="font-bold text-slate-900">{s.name}</div><div className="mt-0.5 text-[10px] text-slate-400">{s.owner} · {s.id}</div></td><td><span className="font-semibold">{s.plan}</span></td><td>{s.listings}</td><td>★ {s.rating}</td><td className="font-semibold text-slate-700">{s.revenue}</td><td><StatusBadge status={s.status}/></td><td><Link href={`/sellers/${s.id}`} className="grid size-8 place-items-center rounded-lg text-slate-400 hover:bg-slate-100"><Icons.chevronRight size={16}/></Link></td></tr>)}</tbody></table></div></div></div>}
