import Link from "next/link";
import { sellers, listings } from "@/data/mock-data";
import { StatusBadge } from "@/components/ui/status-badge";
import { AdminButton } from "@/components/ui/admin-button";
import { Icons } from "@/lib/icons";
export default async function SellerPage({params}:{params:Promise<{sellerId:string}>}){const {sellerId}=await params;const seller=sellers.find(x=>x.id===sellerId)??sellers[0];return <div className="space-y-6"><Link href="/sellers" className="text-xs font-bold text-slate-500">← Back to sellers</Link><div className="flex flex-col gap-4 rounded-xl border border-slate-200 bg-white p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><h1 className="text-xl font-black">{seller.name}</h1><StatusBadge status={seller.status}/></div><p className="mt-1 text-xs text-slate-500">{seller.owner} · {seller.id} · {seller.location}</p></div><div className="flex gap-2"><AdminButton variant="outline">Edit seller</AdminButton><AdminButton variant="danger">Suspend seller</AdminButton></div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Current plan",seller.plan],["Active listings",String(seller.listings)],["Seller rating",`${seller.rating} / 5`],["Plan revenue",seller.revenue]].map(([a,b])=><div className="rounded-xl border border-slate-200 bg-white p-4" key={a}><p className="text-xs text-slate-500">{a}</p><strong className="mt-2 block text-xl font-black">{b}</strong></div>)}</div><div className="grid gap-6 xl:grid-cols-[1.4fr_.6fr]"><section className="overflow-hidden rounded-xl border border-slate-200 bg-white"><div className="px-5 py-4"><h2 className="text-sm font-black">Recent listings</h2></div><div className="table-scroll"><table className="admin-table"><thead><tr><th>Listing</th><th>Price</th><th>Status</th><th/></tr></thead><tbody>{listings.slice(0,4).map(l=><tr key={l.id}><td><div className="font-bold text-slate-900">{l.title}</div><div className="text-[10px] text-slate-400">{l.id}</div></td><td>{l.price}</td><td><StatusBadge status={l.status}/></td><td><Link href={`/listings/${l.id}`} className="text-emerald-700"><Icons.chevronRight size={16}/></Link></td></tr>)}</tbody></table></div></section><aside className="space-y-6"><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-sm font-black">Seller details</h2><div className="mt-4 space-y-4 text-xs"><Row label="Owner" value={seller.owner}/><Row label="Joined" value={seller.joined}/><Row label="Location" value={seller.location}/><Row label="Verification" value={seller.status}/><Row label="Response rate" value="96%"/></div></section><section className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-center gap-2 text-sm font-black"><Icons.shield size={17} className="text-emerald-600"/>Trust & safety</div><p className="mt-3 text-xs leading-5 text-slate-500">No unresolved reports. Seller account is currently in good standing.</p></section></aside></div></div>}
function Row({label,value}:{label:string;value:string}){return <div className="flex justify-between gap-3"><span className="text-slate-500">{label}</span><strong className="text-right text-slate-800">{value}</strong></div>}
