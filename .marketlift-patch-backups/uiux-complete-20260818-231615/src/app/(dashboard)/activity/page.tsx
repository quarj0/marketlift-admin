import { activityLog } from "@/data/mock-data";
import { PageHeader } from "@/components/ui/page-header";
import { TableTools } from "@/components/ui/table-tools";
import { AdminButton } from "@/components/ui/admin-button";
import { Icons } from "@/lib/icons";
export default function ActivityPage(){return <div className="space-y-6"><PageHeader title="Activity logs" description="Audit administrator and automated platform actions." actions={<AdminButton variant="outline"><Icons.download size={15}/> Export audit log</AdminButton>}/><div className="rounded-xl border border-emerald-200 bg-emerald-50/50 p-4"><div className="flex items-start gap-3"><Icons.shield className="mt-0.5 text-emerald-700" size={18}/><div><p className="text-xs font-black text-emerald-900">Immutable audit trail</p><p className="mt-1 text-xs leading-5 text-emerald-800/70">Sensitive administrative operations should be recorded by the backend with actor, target, timestamp and request metadata.</p></div></div></div><div className="overflow-hidden rounded-xl border border-slate-200 bg-white"><TableTools placeholder="Search action, admin or target..."><button className="inline-flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-3 text-xs font-bold text-slate-600"><Icons.calendar size={15}/> Date range</button><button className="inline-flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-3 text-xs font-bold text-slate-600">Action type <Icons.arrowDown size={12}/></button></TableTools><div className="table-scroll"><table className="admin-table"><thead><tr><th>Actor</th><th>Action</th><th>Target</th><th>IP address</th><th>Time</th><th/></tr></thead><tbody>{activityLog.map((a,i)=><tr key={i}><td><div className="flex items-center gap-2"><span className="grid size-8 place-items-center rounded-full bg-slate-100 text-[10px] font-black text-slate-600">{a.admin==="System"?"SY":a.admin.split(" ").map(x=>x[0]).join("")}</span><span className="font-bold text-slate-800">{a.admin}</span></div></td><td>{a.action}</td><td className="font-semibold text-slate-700">{a.target}</td><td className="font-mono text-[11px]">{a.ip}</td><td>{a.time}</td><td><button className="text-slate-400"><Icons.more size={17}/></button></td></tr>)}</tbody></table></div></div></div>}
