"use client";
import { useState } from "react";
import { plans, sellers } from "@/data/mock-data";
import { PageHeader } from "@/components/ui/page-header";
import { AdminButton } from "@/components/ui/admin-button";
import { StatusBadge } from "@/components/ui/status-badge";
import { Dialog } from "@/components/ui/dialog";
import { Icons } from "@/lib/icons";
import { useAdminDemo } from "@/components/admin/admin-demo-provider";
export default function SubscriptionsPage(){const [plan,setPlan]=useState<string|null>(null);const {toast}=useAdminDemo();return <div className="space-y-6"><PageHeader title="Subscriptions" description="Manage seller plans, limits and recurring subscription activity." actions={<AdminButton onClick={()=>setPlan("New plan")}>+ Create plan</AdminButton>}/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{plans.map(p=><article key={p.name} className={`relative rounded-xl border bg-white p-5 transition hover:shadow-lg hover:shadow-slate-950/5 ${p.name==="Professional"?"border-emerald-300 ring-2 ring-emerald-500/5":"border-slate-200"}`}><div className="flex items-center justify-between"><span className="text-xs font-black uppercase tracking-wide text-slate-500">{p.name}</span><span className="rounded-full bg-slate-100 px-2 py-1 text-[9px] font-bold text-slate-500">{p.badge}</span></div><div className="mt-4"><strong className="text-2xl font-black text-slate-950">{p.price}</strong><span className="text-xs text-slate-400">{p.period}</span></div><div className="mt-5 space-y-3 border-t border-slate-100 pt-4 text-xs"><Row label="Active sellers" value={p.sellers.toLocaleString("pt-BR")}/><Row label="Listing limit" value={p.listings}/><Row label="Featured listings" value={p.featured}/></div><button onClick={()=>setPlan(p.name)} className="mt-5 w-full rounded-lg border border-slate-200 py-2 text-xs font-bold text-slate-600 hover:bg-slate-50">Edit plan</button></article>)}</div><section className="overflow-hidden rounded-xl border border-slate-200 bg-white"><div className="flex items-center justify-between px-5 py-4"><div><h2 className="text-sm font-black">Recent subscriptions</h2><p className="mt-0.5 text-xs text-slate-500">Seller plan membership and renewal status</p></div><AdminButton variant="outline" onClick={()=>toast("Subscription export prepared",undefined,"info")}><Icons.download size={15}/> Export</AdminButton></div><div className="table-scroll"><table className="admin-table"><thead><tr><th>Seller</th><th>Plan</th><th>Billing</th><th>Next renewal</th><th>Status</th></tr></thead><tbody>{sellers.map((s,i)=><tr key={s.id}><td><div className="font-bold text-slate-900">{s.name}</div><div className="text-[10px] text-slate-400">{s.id}</div></td><td>{s.plan}</td><td>{s.plan==="Business"?"R$ 599/mo":s.plan==="Professional"?"R$ 249/mo":"R$ 79/mo"}</td><td>{`${22-i} Sep 2026`}</td><td><StatusBadge status={s.status==="Suspended"?"Suspended":"Active"}/></td></tr>)}</tbody></table></div></section><Dialog open={plan!==null} onClose={()=>setPlan(null)} title={plan==="New plan"?"Create subscription plan":`Edit ${plan}`} description="Configure seller plan pricing, listing limits and featured-listing allowances." footer={<><AdminButton variant="outline" onClick={()=>setPlan(null)}>Cancel</AdminButton><AdminButton onClick={()=>{toast(plan==="New plan"?"Plan created":"Plan updated","Subscription plan configuration saved.");setPlan(null)}}>Save plan</AdminButton></>}><div className="grid gap-4 sm:grid-cols-2"><Field label="Plan name" value={plan==="New plan"?"":plan??""}/><Field label="Monthly price" value="R$ 249"/><Field label="Listing limit" value="50"/><Field label="Featured credits" value="5"/></div></Dialog></div>}
function Row({label,value}:{label:string;value:string}){return <div className="flex justify-between"><span className="text-slate-500">{label}</span><strong className="text-slate-800">{value}</strong></div>}
function Field({label,value}:{label:string;value:string}){return <label><span className="mb-2 block text-xs font-bold text-slate-700">{label}</span><input defaultValue={value} className="h-10 w-full rounded-lg border border-slate-200 px-3 text-sm outline-none focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/10"/></label>}
