"use client";
import { useState } from "react";
import { categories as initial } from "@/data/mock-data";
import { PageHeader } from "@/components/ui/page-header";
import { AdminButton } from "@/components/ui/admin-button";
import { Dialog } from "@/components/ui/dialog";
import { useAdminDemo } from "@/components/admin/admin-demo-provider";
export default function CategoriesPage(){const [categories,setCategories]=useState(initial);const [open,setOpen]=useState(false);const [name,setName]=useState("");const {toast}=useAdminDemo();const toggle=(slug:string)=>{setCategories(c=>c.map(x=>x.slug===slug?{...x,active:!x.active}:x));toast("Category updated","Category visibility updated.")};return <div className="space-y-6"><PageHeader title="Categories" description="Manage marketplace category structure and visibility." actions={<AdminButton onClick={()=>setOpen(true)}>+ Add category</AdminButton>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{categories.map(c=><div key={c.slug} className="group rounded-xl border border-slate-200 bg-white p-5 transition hover:-translate-y-0.5 hover:shadow-lg hover:shadow-slate-950/5"><div className="flex items-start justify-between"><span className="grid size-11 place-items-center rounded-xl bg-slate-50 text-xl">{c.icon}</span><button onClick={()=>toggle(c.slug)} className="rounded-lg border border-slate-200 px-2.5 py-1.5 text-[10px] font-black text-slate-600 hover:bg-slate-50">{c.active?"Hide":"Show"}</button></div><h2 className="mt-4 text-sm font-black text-slate-900">{c.name}</h2><p className="mt-1 text-[11px] text-slate-400">/{c.slug}</p><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><div><p className="text-[10px] text-slate-400">Listings</p><strong className="text-sm text-slate-800">{c.listings.toLocaleString("pt-BR")}</strong></div><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${c.active?"bg-emerald-50 text-emerald-700":"bg-slate-100 text-slate-500"}`}>{c.active?"Active":"Hidden"}</span></div></div>)}</div><Dialog open={open} onClose={()=>setOpen(false)} title="Add category" description="Create a new top-level marketplace category." footer={<><AdminButton variant="outline" onClick={()=>setOpen(false)}>Cancel</AdminButton><AdminButton disabled={name.trim().length<2} onClick={()=>{const clean=name.trim();setCategories(c=>[...c,{name:clean,slug:clean.toLowerCase().replace(/[^a-z0-9]+/g,"-"),listings:0,active:true,icon:"📦"}]);setName("");setOpen(false);toast("Category created",`${clean} is now visible in the category catalog.`)}}>Create category</AdminButton></>}><label className="block"><span className="mb-2 block text-xs font-bold text-slate-700">Category name</span><input data-dialog-autofocus value={name} onChange={e=>setName(e.target.value)} className="h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/10" placeholder="e.g. Sports & Outdoors"/></label></Dialog></div>}
