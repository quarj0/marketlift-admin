"use client";
import {SafeLink} from "@/components/ui/safe-link";import {PageHeader} from "@/components/ui/page-header";import {DataTable} from "@/components/ui/data-table";import {StatusBadge} from "@/components/ui/status-badge";import {AdminButton} from "@/components/ui/admin-button";import {Icons} from "@/lib/icons";import {exportCsv} from "@/lib/csv-export";import {useAdminData} from "@/components/admin/admin-data-provider";
const brl=(v:number)=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(v);
export default function PaymentsPage(){const {payments,paymentSummary}=useAdminData();return <div className="space-y-6"><PageHeader title="Payments" description="Monitor Marketlift subscription and promotion transactions." actions={<AdminButton variant="outline" onClick={()=>exportCsv("marketlift-payments.csv",["ID","Reference","Seller","Purpose","Method","Amount","Status","Provider","Provider order","Date"],payments.map(p=>[p.id,p.reference,p.seller,p.purpose,p.method,p.amount,p.status,p.provider,p.providerOrderId,p.date]))}><Icons.download size={16}/> Export</AdminButton>}/><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[["Paid volume",brl(paymentSummary.paidTotal),`${paymentSummary.paidCount} paid`],["Refunded",brl(paymentSummary.refundedTotal),"Recorded refunds"],["Success rate",`${paymentSummary.successRate.toFixed(1)}%`,`${paymentSummary.failedCount} failed`],["Pending",String(paymentSummary.pendingCount),"Awaiting completion"]].map(([a,b,c])=><div key={a} className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-xs font-semibold text-slate-500">{a}</p><strong className="mt-2 block text-xl font-black">{b}</strong><p className="mt-1 text-[10px] text-slate-400">{c}</p></div>)}</div><DataTable rows={payments} searchText={p=>`${p.id} ${p.reference} ${p.seller} ${p.type} ${p.method} ${p.status}`} searchPlaceholder="Search payment, seller or transaction ID…" statusOf={p=>p.status} statuses={["Paid","Pending","Failed","Refunded","Cancelled"]} selectedLabel="payments" columns={[{key:"payment",label:"Payment",cell:p=><div><div className="font-bold text-slate-900">{p.reference||p.id}</div><div className="mt-0.5 text-[10px] text-slate-400">{p.seller} · {p.id}</div></div>},{key:"type",label:"Product",cell:p=>p.type},{key:"amount",label:"Amount",cell:p=><strong className="text-slate-800">{p.amount}</strong>},{key:"method",label:"Method",cell:p=>p.method},{key:"date",label:"Date",cell:p=>p.date},{key:"status",label:"Status",cell:p=><StatusBadge status={p.status}/>},{key:"action",label:"",cell:p=><SafeLink href={`/payments/${p.id}`} aria-label={`View payment ${p.reference||p.id}`} className="grid size-8 place-items-center rounded-lg text-slate-500 hover:bg-slate-100"><Icons.chevronRight size={16}/></SafeLink>}]}/></div>}
