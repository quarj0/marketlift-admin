"use client";
import { useParams } from "next/navigation";
import { SafeLink } from "@/components/ui/safe-link";
import { LiveStatusBadge } from "@/components/ui/live-status-badge";
import { EntityActions, MoveListingToReviewAction } from "@/components/admin/entity-actions";
import { MarketplaceImage } from "@/components/admin/marketplace-image";
import { PublicListingLink, PublicSellerLink } from "@/components/admin/public-marketplace-link";
import { useAdminData } from "@/components/admin/admin-data-provider";
export default function ListingDetailPage(){const {listingId}=useParams<{listingId:string}>();const {listings,sellers,reports,moderationCases,loading}=useAdminData();const listing=listings.find((x)=>x.id===listingId||x.publicSlug===listingId);if(loading&&!listing)return <div className="rounded-xl border bg-white p-8 text-sm text-slate-500">Loading listing…</div>;if(!listing)return <div className="rounded-xl border bg-white p-8 text-center"><h1 className="text-lg font-black">Listing not found</h1><SafeLink href="/listings" className="mt-4 inline-flex text-sm font-bold text-emerald-700">Back to listings</SafeLink></div>;const seller=sellers.find((s)=>s.id===listing.publicSellerId);const relatedReports=reports.filter((r)=>r.type==="Listing"&&r.targetId===listing.id);const moderation=moderationCases.find((m)=>m.listingId===listing.id);return <div className="space-y-6"><SafeLink href="/listings" className="text-xs font-bold text-slate-500">← Back to listings</SafeLink><div className="flex flex-col gap-4 rounded-xl border border-slate-200 bg-white p-5 lg:flex-row lg:items-center lg:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-xl font-black text-slate-950">{listing.title}</h1><LiveStatusBadge kind="listing" id={listing.id} status={listing.status}/></div><p className="mt-1 text-xs text-slate-500">{listing.id} · {listing.category} · {listing.created}</p></div><EntityActions kind="listing" id={listing.id} name={listing.title} status={listing.status} publicSlug={listing.publicSlug}/></div><div className="grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><section className="space-y-6"><div className="overflow-hidden rounded-xl border border-slate-200 bg-white"><div className="grid gap-2 p-4 sm:grid-cols-2">{listing.images.length?listing.images.map((src,index)=><MarketplaceImage key={src+index} src={src} alt={`${listing.title} image ${index+1}`} className="h-60 w-full rounded-lg object-cover" fallbackClassName="h-60 w-full rounded-lg"/>):<div className="grid h-60 place-items-center rounded-lg bg-slate-100 text-sm text-slate-400">No images</div>}</div><div className="border-t border-slate-100 p-5"><h2 className="text-sm font-black">Listing content</h2><p className="mt-3 whitespace-pre-wrap text-sm leading-6 text-slate-600">{listing.description}</p></div></div><div className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-sm font-black">Moderation history</h2>{moderation?<div className="mt-4 space-y-3 text-xs"><Row label="Case status" value={moderation.status}/><Row label="Source" value={moderation.source}/><Row label="Review reason" value={moderation.reviewReason}/><Row label="Decision reason" value={moderation.decisionReason||"—"}/><Row label="Opened" value={moderation.openedAt}/><Row label="Decided by" value={moderation.decidedBy||"—"}/></div>:<p className="mt-3 text-xs text-slate-500">No moderation case is recorded for this listing.</p>}</div></section><aside className="space-y-6"><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-sm font-black">Listing details</h2><div className="mt-4 space-y-4 text-xs"><Row label="Price" value={listing.price}/><Row label="Seller" value={listing.seller}/><Row label="Condition" value={listing.condition}/><Row label="Location" value={listing.location}/><Row label="Views" value={String(listing.views)}/><Row label="Favorites" value={String(listing.favorites)}/><Row label="Inquiries" value={String(listing.inquiries)}/><Row label="Reports" value={String(relatedReports.length)}/></div><div className="mt-5 flex flex-col gap-2"><PublicListingLink title={listing.title} slug={listing.publicSlug}/>{seller&&<PublicSellerLink sellerId={seller.id} sellerName={seller.name}/>}</div></section><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-sm font-black">Queue control</h2><p className="mt-2 text-xs leading-5 text-slate-500">Move an eligible listing into moderation when it needs administrator review.</p><div className="mt-4"><MoveListingToReviewAction id={listing.id} status={listing.status}/></div></section>{relatedReports.length>0&&<section className="rounded-xl border border-orange-200 bg-orange-50/40 p-5"><h2 className="text-sm font-black text-orange-950">Related reports</h2><div className="mt-3 space-y-2">{relatedReports.map((r)=><SafeLink key={r.id} href={`/reports/${r.id}`} className="block rounded-lg bg-white/70 p-3 text-xs font-bold text-orange-900">{r.reference} · {r.reason}</SafeLink>)}</div></section>}</aside></div></div>}
function Row({label,value}:{label:string;value:string}){return <div className="flex justify-between gap-3"><span className="text-slate-500">{label}</span><strong className="max-w-[65%] break-words text-right text-slate-800">{value}</strong></div>}
